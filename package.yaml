name: sgf-parsing
version: 1.2.0.5

library:
  exposed-modules: Sgf
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - base
    - containers
    - text
    - array
    - ghc
    - megaparsec
    - mtl
    - regex-base
    - regex-tdfa
    - hspec

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - base
      - containers
      - sgf-parsing
      - hspec
